/**
 * 抛弃服务器(DISCARD) {@link com.yuxing.trainee.netty.discard.DiscardServer}: 该服务将抛弃任何接收到的数据，而不响应
 * 在启动服务后，可以通过 `telnet localhost 8899` 来验证该服务是否已正常运行
 *
 * @author yuxing
 * @since 2021/12/29
 */
package com.yuxing.trainee.netty.discard;